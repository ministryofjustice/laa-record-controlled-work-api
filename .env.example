# 1Password is used to store sensitive secrets that might be needed locally.
# ALL SECRETS MUST BE MIRRORED IN DOCKER COMPOSE FILE.
#
# One password environment variables are injected when you run
# `op run --env-file=.env -- `, followed by a command which uses .env
#
# https://developer.1password.com/docs/cli/secret-reference-syntax

GITHUB_TOKEN="op://Employee/Github/PAT_TOKEN"
ENTRA_TENANT_ID="op://LAA Record Controlled Work/RCW-API/ENTRA_TENANT_ID"

# --- set these to use Entra for tokens with docker compose ---
# --- defaults in docker-compose.yml apply if unset ---
# ISSUER="op://LAA Record Controlled Work/RCW-API/ENTRA_ISSUER"
# AUDIENCE="op://LAA Record Controlled Work/RCW-API/ENTRA_CLIENT_ID"

# RCW_API_CLIENT_ID="op://LAA Record Controlled Work/RCW-API/ENTRA_CLIENT_ID"
# RCW_API_CLIENT_SECRET="op://LAA Record Controlled Work/RCW-API/ENTRA_CLIENT_SECRET"

# DATASTORE_ENTRA_ISSUER_URI="op://LAA Record Controlled Work/RCW-API/ENTRA_ISSUER"
# DATASTORE_TOKEN_URI="op://LAA Record Controlled Work/RCW-API/DS_API_ENTRA_TOKEN_URI"
# DATASTORE_ENTRA_AUDIENCE="op://LAA Record Controlled Work/RCW-API/DS_API_ENTRA_CLIENT_ID"
# DATASTORE_OAUTH_SCOPES="op://LAA Record Controlled Work/RCW-API/DS_API_ENTRA_OAUTH_SCOPES"
