info:
  name: applications
  type: folder
  seq: 1

request:
  auth:
    type: bearer
    token: "{{accessToken}}"
  scripts:
    - type: before-request
      code: |-
        await bru.runRequest("Get Local Token")
